version: "3"

services:
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: rabbitmq
    hostname: rabbitmq-management-client
    ports:
      - 5672:5672
      - 15672:15672
    networks:
      - rmq_net

networks:
  rmq_net:
    driver: bridge
